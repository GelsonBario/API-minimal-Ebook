{
  "name": "books-api",
  "version": "1.0.0",
  "description": "Uma API simples para gerenciar livros, construída com Node.js, Fastify e TypeScript.",
  "main": "dist/server.js",
  "scripts": {
    "dist": "tsup src --out-dir dist",
    "start:dev": "tsx watch --env-file=.env src/server.ts",
    "start:watch": "tsx watch --env-file=.env src/server.ts",
    "start": "node dist/server.js",
    "build-and-start": "npm run dist && npm run start"
  },
  "keywords": [
    "nodejs",
    "typescript",
    "fastify",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "tsup": "^8.0.2",
    "tsx": "^4.7.1",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "fastify": "^5.4.0"
  }
}
